# Task CLI Project
# Task Tracker CLI - Python

Sample solution for the **Task Tracker CLI** challenge from [roadmap.sh](https://roadmap.sh/projects/task-tracker).

This project is a **Python command-line tool** to manage tasks using a JSON file for storage. You can **add, update, delete, mark, and list tasks** directly from the terminal.

---

## How to Run

1. Clone the repository:

```bash
git clone https://github.com/kirabo22/project-python
cd project-python

This is a **Command-Line Interface (CLI) Task Tracker** built with Python.  
It allows you to **add, update, delete, and manage tasks** directly from the terminal, and stores your tasks in a JSON file.

---

## Features

- Add new tasks with multi-word descriptions  
- Update existing tasks  
- Delete tasks  
- Mark tasks as **in-progress** or **done**  
- List all tasks with status (`todo`, `in-progress`, `done`)  
- Timestamps for task creation and updates (`createdAt`, `updatedAt`)  
- Simple CLI interface with help menu  

---

## Getting Started

1. Clone the repository:

```bash
git clone https://github.com/kirabo22/project-python
